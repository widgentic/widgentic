/**
 * @widgentic/mcp — building blocks for hosting the widgentic engine over
 * MCP: the widgentic tool-output convention, the tool handlers, the app
 * template, action execution, guarded fetching and the edge helpers. The
 * official-SDK assembly lives behind `@widgentic/mcp/sdk`; the per-principal
 * store and secrets behind `@widgentic/mcp/store` and `@widgentic/mcp/secrets`.
 */
export * from "./output/index.js";
export * from "./server/index.js";

{
  "name": "@widgentic-examples/shared",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Wiring shared by the example hosts: designer mount discipline, WebMCP designer sources and the authoring-API client. Palettes are never here — a page derives its own from @widgentic/designer's chromeCss(CHROME_DEFAULTS).",
  "dependencies": {
    "@widgentic/core": "^0.7.0",
    "@widgentic/designer": "^0.7.0",
    "@widgentic/mcp": "^0.7.0",
    "@widgentic/webmcp": "^0.1.0"
  },
  "exports": {
    "./designers": "./designers.ts",
    "./client": "./client.ts",
    "./webmcp": "./webmcp.ts"
  }
}
